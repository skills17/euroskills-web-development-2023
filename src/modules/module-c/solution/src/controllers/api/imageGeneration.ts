import {Request, Response} from "express";

function generate(req: Request, res: Response) {
    res.json({});
}

function getJobStatus(req: Request, res: Response) {
    res.json({});
}

function getResult(req: Request, res: Response) {
    res.json({});
}

function upscale(req: Request, res: Response) {
    res.json({});
}

function zoomIn(req: Request, res: Response) {
    res.json({});
}

function zoomOut(req: Request, res: Response) {
    res.json({});
}

export default {
    generate,
    getJobStatus,
    getResult,
    upscale,
    zoomIn,
    zoomOut,
};
